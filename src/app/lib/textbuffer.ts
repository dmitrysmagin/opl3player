export const PALETTE = [
  '#000000', '#0000AA', '#00AA00', '#00AAAA',
  '#AA0000', '#AA00AA', '#AA5500', '#AAAAAA',
  '#555555', '#5555FF', '#55FF55', '#55FFFF',
  '#FF5555', '#FF55FF', '#FFFF55', '#FFFFFF',
];

export const COLOR = {
  BLACK: 0, BLUE: 1, GREEN: 2, CYAN: 3,
  RED: 4, MAGENTA: 5, BROWN: 6, LIGHTGREY: 7,
  DARKGREY: 8, LIGHTBLUE: 9, LIGHTGREEN: 10, LIGHTCYAN: 11,
  LIGHTRED: 12, LIGHTMAGENTA: 13, YELLOW: 14, WHITE: 15,
} as const;

const CP437 = new Uint16Array(256);
for (let i = 0x20; i <= 0x7E; i++) CP437[i] = i;
const M = (c: number, u: number) => { CP437[c] = u; };
M(0x00, 0x0000); M(0x01, 0x263A); M(0x02, 0x263B); M(0x03, 0x2665);
M(0x04, 0x2666); M(0x05, 0x2663); M(0x06, 0x2660); M(0x07, 0x2022);
M(0x08, 0x25D8); M(0x09, 0x25CB); M(0x0A, 0x25D9); M(0x0B, 0x2642);
M(0x0C, 0x2640); M(0x0D, 0x266A); M(0x0E, 0x266B); M(0x0F, 0x263C);
M(0x10, 0x25BA); M(0x11, 0x25C4); M(0x12, 0x2195); M(0x13, 0x203C);
M(0x14, 0x00B6); M(0x15, 0x00A7); M(0x16, 0x25AC); M(0x17, 0x21A8);
M(0x18, 0x2191); M(0x19, 0x2193); M(0x1A, 0x2192); M(0x1B, 0x2190);
M(0x1C, 0x221F); M(0x1D, 0x2194); M(0x1E, 0x25B2); M(0x1F, 0x25BC);
M(0x7F, 0x2302);
M(0x80, 0x00C7); M(0x81, 0x00FC); M(0x82, 0x00E9); M(0x83, 0x00E2);
M(0x84, 0x00E4); M(0x85, 0x00E0); M(0x86, 0x00E5); M(0x87, 0x00E7);
M(0x88, 0x00EA); M(0x89, 0x00EB); M(0x8A, 0x00E8); M(0x8B, 0x00EF);
M(0x8C, 0x00EE); M(0x8D, 0x00EC); M(0x8E, 0x00C4); M(0x8F, 0x00C5);
M(0x90, 0x00C9); M(0x91, 0x00E6); M(0x92, 0x00C6); M(0x93, 0x00F4);
M(0x94, 0x00F6); M(0x95, 0x00F2); M(0x96, 0x00FB); M(0x97, 0x00F9);
M(0x98, 0x00FF); M(0x99, 0x00D6); M(0x9A, 0x00DC); M(0x9B, 0x00A2);
M(0x9C, 0x00A3); M(0x9D, 0x00A5); M(0x9E, 0x20A7); M(0x9F, 0x0192);
M(0xA0, 0x00E1); M(0xA1, 0x00ED); M(0xA2, 0x00F3); M(0xA3, 0x00FA);
M(0xA4, 0x00F1); M(0xA5, 0x00D1); M(0xA6, 0x00AA); M(0xA7, 0x00BA);
M(0xA8, 0x00BF); M(0xA9, 0x2310); M(0xAA, 0x00AC); M(0xAB, 0x00BD);
M(0xAC, 0x00BC); M(0xAD, 0x00A1); M(0xAE, 0x00AB); M(0xAF, 0x00BB);
M(0xB0, 0x2591); M(0xB1, 0x2592); M(0xB2, 0x2593); M(0xB3, 0x2502);
M(0xB4, 0x2524); M(0xB5, 0x2561); M(0xB6, 0x2562); M(0xB7, 0x2556);
M(0xB8, 0x2555); M(0xB9, 0x2563); M(0xBA, 0x2551); M(0xBB, 0x2557);
M(0xBC, 0x255D); M(0xBD, 0x255C); M(0xBE, 0x255B); M(0xBF, 0x2510);
M(0xC0, 0x2514); M(0xC1, 0x2534); M(0xC2, 0x252C); M(0xC3, 0x251C);
M(0xC4, 0x2500); M(0xC5, 0x253C); M(0xC6, 0x255E); M(0xC7, 0x255F);
M(0xC8, 0x255A); M(0xC9, 0x2554); M(0xCA, 0x2569); M(0xCB, 0x2566);
M(0xCC, 0x2560); M(0xCD, 0x2550); M(0xCE, 0x256C); M(0xCF, 0x2567);
M(0xD0, 0x2568); M(0xD1, 0x2564); M(0xD2, 0x2565); M(0xD3, 0x2559);
M(0xD4, 0x2558); M(0xD5, 0x2552); M(0xD6, 0x2553); M(0xD7, 0x256B);
M(0xD8, 0x256A); M(0xD9, 0x2518); M(0xDA, 0x250C); M(0xDB, 0x2588);
M(0xDC, 0x258C); M(0xDD, 0x2590); M(0xDE, 0x2580); M(0xDF, 0x03B1);
M(0xE0, 0x00DF); M(0xE1, 0x0393); M(0xE2, 0x03C0); M(0xE3, 0x03A3);
M(0xE4, 0x03C3); M(0xE5, 0x00B5); M(0xE6, 0x03C4); M(0xE7, 0x03A6);
M(0xE8, 0x0398); M(0xE9, 0x03A9); M(0xEA, 0x03B4); M(0xEB, 0x221E);
M(0xEC, 0x03C6); M(0xED, 0x03B5); M(0xEE, 0x2229); M(0xEF, 0x2261);
M(0xF0, 0x00B1); M(0xF1, 0x2265); M(0xF2, 0x2264); M(0xF3, 0x2320);
M(0xF4, 0x2321); M(0xF5, 0x00F7); M(0xF6, 0x2248); M(0xF7, 0x00B0);
M(0xF8, 0x2219); M(0xF9, 0x00B7); M(0xFA, 0x221A); M(0xFB, 0x207F);
M(0xFC, 0x00B2); M(0xFD, 0x25A0); M(0xFE, 0x00A0);

export function cp437toUnicode(c: number): string {
  const u = CP437[c];
  return u <= 0xFFFF ? String.fromCharCode(u) : '';
}

export class TextBuffer {
  chars: Uint8Array;
  attrs: Uint8Array;
  width: number;
  height: number;

  constructor(width: number, height: number) {
    this.width = width;
    this.height = height;
    this.chars = new Uint8Array(width * height);
    this.attrs = new Uint8Array(width * height);
    this.clear();
  }

  clear() {
    this.chars.fill(0x20);
    this.attrs.fill(0x07);
  }

  inBounds(x: number, y: number): boolean {
    return x >= 0 && x < this.width && y >= 0 && y < this.height;
  }

  setCell(x: number, y: number, char: number, fg: number, bg: number = 0) {
    if (!this.inBounds(x, y)) return;
    const idx = y * this.width + x;
    this.chars[idx] = char;
    this.attrs[idx] = (bg << 4) | fg;
  }

  getCell(x: number, y: number): { char: number; fg: number; bg: number } {
    if (!this.inBounds(x, y)) return { char: 0x20, fg: 7, bg: 0 };
    const idx = y * this.width + x;
    const attr = this.attrs[idx];
    return { char: this.chars[idx], fg: attr & 0x0F, bg: (attr >> 4) & 0x0F };
  }

  drawChar(x: number, y: number, char: number, fg: number, bg: number = 0) {
    this.setCell(x, y, char, fg, bg);
  }

  drawString(x: number, y: number, str: string, fg: number, bg: number = 0) {
    for (let i = 0; i < str.length; i++) {
      this.drawChar(x + i, y, str.charCodeAt(i), fg, bg);
    }
  }

  drawStringFromCodes(x: number, y: number, codes: number[], fg: number, bg: number = 0) {
    for (let i = 0; i < codes.length; i++) {
      this.drawChar(x + i, y, codes[i], fg, bg);
    }
  }

  fillRect(x: number, y: number, w: number, h: number, char: number, fg: number, bg: number = 0) {
    for (let dy = 0; dy < h; dy++) {
      for (let dx = 0; dx < w; dx++) {
        this.drawChar(x + dx, y + dy, char, fg, bg);
      }
    }
  }

  drawHLine(x: number, y: number, len: number, char: number, fg: number, bg: number = 0) {
    for (let i = 0; i < len; i++) {
      this.drawChar(x + i, y, char, fg, bg);
    }
  }

  drawVLine(x: number, y: number, len: number, char: number, fg: number, bg: number = 0) {
    for (let i = 0; i < len; i++) {
      this.drawChar(x, y + i, char, fg, bg);
    }
  }

  drawBox(x: number, y: number, w: number, h: number, fg: number, bg: number = 0, double: boolean = false) {
    const tl = double ? 0xC9 : 0xDA;
    const tr = double ? 0xBB : 0xBF;
    const bl = double ? 0xC8 : 0xC0;
    const br = double ? 0xD9 : 0xD9;
    const hLine = double ? 0xCD : 0xC4;
    const vLine = double ? 0xBA : 0xB3;

    this.drawChar(x, y, tl, fg, bg);
    this.drawChar(x + w - 1, y, tr, fg, bg);
    this.drawChar(x, y + h - 1, bl, fg, bg);
    this.drawChar(x + w - 1, y + h - 1, br, fg, bg);

    this.drawHLine(x + 1, y, w - 2, hLine, fg, bg);
    this.drawHLine(x + 1, y + h - 1, w - 2, hLine, fg, bg);
    this.drawVLine(x, y + 1, h - 2, vLine, fg, bg);
    this.drawVLine(x + w - 1, y + 1, h - 2, vLine, fg, bg);
  }

  drawLevelBar(x: number, y: number, level: number, maxH: number, fg: number, bg: number = 0) {
    const blocks = [0x20, 0xB0, 0xB1, 0xB2, 0xDB];
    const filled = Math.max(0, Math.min(maxH, level));
    const empty = maxH - filled;

    for (let row = 0; row < empty; row++) {
      this.drawChar(x, y + row, 0x20, fg, bg);
    }
    for (let row = 0; row < filled; row++) {
      const intensity = Math.min(4, Math.floor((row / maxH) * 5) + 1);
      this.drawChar(x, y + empty + row, blocks[intensity], fg, bg);
    }
  }

}
